{
  "framework": "nextjs",
  "installCommand": "printenv | grep '^NEXT_PUBLIC_' > ./frontend/app/.env.local && pnpm -w i --frozen-lockfile",
  "buildCommand": "pnpm -w --filter @liquity2/app build",
  "outputDirectory": ".next"
}
