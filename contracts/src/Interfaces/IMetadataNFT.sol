// SPDX-License-Identifier: MIT
pragma solidity 0.8.24;

interface IMetadataNFT {
    // Define functions if any are called by TroveNFT or other contracts directly.
    // If it's primarily for TroveNFT's internal use and set via constructor,
    // this interface might remain minimal or even empty.
    // For now, keeping it empty as a placeholder to satisfy imports.
    // function tokenURI(uint256 tokenId) external view returns (string memory);
    // function contractURI() external view returns (string memory);
}
